from typing import Self

import numpy as np

class Buffer:
    def load_image(self, key: str, *, output_key: str = ...) -> Self: ...
    def image_resize_smallest_side(self, key: str, size: int) -> Self: ...
    def image_center_crop(self, key: str, w: int, h: int) -> Self: ...
    def __iter__(self) -> ...: ...

def buffer_from_vector(data: list[dict[str, np.ndarray | bytes]]) -> Buffer: ...
